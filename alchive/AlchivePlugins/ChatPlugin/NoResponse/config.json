{
    "schema": 1,
    "description": "Response to user query about its query that it is non Biology related.",
    "execution_settings": {
      "default": {
        "max_tokens": 200,
        "temperature": 0.7,
        "top_p": 0.0,
        "presence_penalty": 0.0,
        "frequency_penalty": 0.0
      }
    },
    "input_variables": [
        {
            "name":"user_query",
            "description": "user query.",
            "default":""
        }
    ]
}